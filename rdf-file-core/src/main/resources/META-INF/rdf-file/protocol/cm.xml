<protocol name="cm">
    <head>
        <row output="${columnInfo.horizontal(head,desc)}" />
        <row>
            <column>
                <output>${column.value()}</output>
            </column>
        </row>
        <row output="${columnInfo.horizontal(body,desc)}" />
    </head>
    <body>
        <row>
            <column>
                <output>${column.value()}</output>
            </column>
        </row>
    </body>
    <tail>
        <row output="${columnInfo.horizontal(tail,desc)}" />
        <row>
            <column>
                <output>${column.value()}</output>
            </column>
        </row>
    </tail>
</protocol>